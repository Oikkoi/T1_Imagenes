#P1:
### Boceto 1:
La función `ColorSaturationHSV` recibe dos argumentos. El primero, el nombre de archivo de la imagen (que debe estar dentro de la misma carpeta que `P1.py`). El segundo es una lista con pares ordenados *`(h_i, m_i)`*

Enlace al repo: [Enlace](https://github.com/Oikkoi/T1_Imagenes.git)